# contractReviewSystem UI

Vue 3 frontend for the contract review backend.

## Run

```bash
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost:8080`. Change `VITE_BACKEND_URL` in `.env.development` if the backend runs on another address.
